function showAbout() {
    $('html, body').animate({
        scrollTop: $("#about-info").offset().top - 50
    }, 500);
}

function showWhat() {
    $('html, body').animate({
        scrollTop: $("#what").offset().top
    }, 500);
}

function showWhen() {
    $('html, body').animate({
        scrollTop: $("#when").offset().top
    }, 500);
}

function showWhere() {
    $('html, body').animate({
        scrollTop: $("#where").offset().top
    }, 500);
}

function showRegistration() {
    $('html, body').animate({
        scrollTop: $("#registration").offset().top
    }, 500);
}

function showSponsors() {
    $('html, body').animate({
        scrollTop: $("#sponsors").offset().top
    }, 500);
}


